{
  "rules": {
    "votes": {
      ".read": true,
      "$questionId": {
        "t": {
          ".write": true,
          ".validate": "newData.isNumber() && ((!data.exists() && newData.val() === 1) || newData.val() === data.val() + 1)"
        },
        "f": {
          ".write": true,
          ".validate": "newData.isNumber() && ((!data.exists() && newData.val() === 1) || newData.val() === data.val() + 1)"
        }
      }
    }
  }
}
